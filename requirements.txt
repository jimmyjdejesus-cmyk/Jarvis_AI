# Core dependencies for Jarvis AI
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-dotenv==1.0.0
starlette==0.27.0

# HTTP client for OpenRouter
requests==2.31.0
aiohttp==3.9.1

# MCP and model routing
websockets==12.0
asyncio-mqtt==0.16.1

# Logging and utilities
pyyaml==6.0.1
colorama==0.4.6

# Optional: For advanced agent features
langchain==0.1.0
langchain-community==0.0.10
crewai==0.11.0

# Local model support
ollama==0.2.1

# Backports / compatibility
typing-extensions>=4.7.1

# Websocket client (module name: websocket)
websocket-client>=1.6.0
responses==0.25.8
