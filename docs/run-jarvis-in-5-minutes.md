# Run Jarvis in 5 Minutes

1. Install dependencies:
   ```bash
   pip install "jarvis-ai[ui]"
   ```
2. Launch the CLI with a mission objective:
   ```bash
   jarvis run "hello world"
   ```
3. Chat with Jarvis and explore the demo pruning workflow using:
   ```bash
   jarvis demo pruning
   ```
