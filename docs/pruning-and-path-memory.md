# Pruning & Path Memory

Jarvis tracks exploration paths and prunes less useful branches to stay efficient.
Use `jarvis demo pruning` to visualize how paths are trimmed and remembered for future runs.
